# Local Vite reads Frontend/.env
# Netlify: Site settings → Environment variables → set these, then Redeploy
# (Frontend/.env is gitignored and will NOT be on Netlify)

VITE_BACKEND_URL=https://monorom-retail-backend.vercel.app
VITE_UPLOAD_BACKEND_URL=https://api.monorom.store
